{
  "name": "unicity-bridge-contracts-tron",
  "version": "0.1.0",
  "private": true,
  "description": "Tron (TVM) lock contract for bridging USDT into Unicity",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "ethers": "^6.16.0",
    "tronweb": "^6.3.0"
  }
}
